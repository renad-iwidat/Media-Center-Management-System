# 🚀 دليل سريع: كيف يُنشر الخبر؟

## 🎯 الإجابة المختصرة

الخبر يصل لقسم النشر (`published_items`) عندما:

```
✅ المحتوى ≥ 100 حرف
✅ الصورة موجودة
✅ المحرر وافق عليه (أو النظام للأخبار الآلية)
```

---

## 📊 مسارين للنشر

### 1️⃣ المسار التحريري (يدوي) 🔵

**التصنيفات**: سياسي، محلي، دولي

```
الخبر → قسم التحرير (pending)
     ↓
المحرر يراجع ويوافق ✅
     ↓
قسم النشر (published_items)
```

**كيف يوافق المحرر؟**
1. افتح "قسم التحرير"
2. اختر الخبر
3. راجعه
4. اضغط "موافقة" ✅

---

### 2️⃣ المسار الآلي (تلقائي) ⚡

**التصنيفات**: رياضة، اقتصاد، صحة، علوم، فن، بيئة، غذاء

```
الخبر → قسم التحرير (pending)
     ↓
النظام يوافق تلقائياً (كل 10 دقائق)
     ↓
قسم النشر (published_items)
```

**لا يحتاج لموافقة المحرر!** ✨

---

## ✅ شروط النشر

| الشرط | القيمة المطلوبة |
|-------|-----------------|
| **المحتوى** | ≥ **100 حرف** |
| **الصورة** | يجب أن تكون **موجودة** |
| **الموافقة** | من المحرر أو النظام |
| **لم يُرفض** | `status ≠ 'rejected'` |

---

## 🔄 حالات الخبر

| الحالة | المعنى | الخطوة التالية |
|--------|--------|----------------|
| **incomplete** ❌ | ناقص | المحرر يكمله |
| **pending** ⏳ | ينتظر | المحرر يراجعه |
| **approved** ✅ | تمت الموافقة | يُنشر! |
| **rejected** ❌ | تم الرفض | لا يُنشر |

---

## 💡 أمثلة سريعة

### مثال 1: خبر رياضي

```
1. محتوى: 250 حرف ✅
2. صورة: موجودة ✅
3. التصنيف: رياضة ⚡

النتيجة: ينشر تلقائياً خلال 10 دقائق ✨
```

### مثال 2: خبر سياسي

```
1. محتوى: 300 حرف ✅
2. صورة: موجودة ✅
3. التصنيف: سياسي 🔵

النتيجة: ينتظر موافقة المحرر ⏳
```

### مثال 3: خبر ناقص

```
1. محتوى: 50 حرف ❌
2. صورة: مفقودة ❌

النتيجة: يذهب لقائمة "أخبار غير مكتملة" ⚠️
```

---

## 🎯 خطوات النشر اليدوية

### للمحرر:

#### إذا كان الخبر مكتمل:

1. افتح **"قسم التحرير"**
2. اختر الخبر من القائمة
3. راجع المحتوى والصورة
4. اضغط **"موافقة"** ✅
5. يظهر فوراً في **"قسم النشر"**

#### إذا كان الخبر ناقص:

1. افتح **"أخبار غير مكتملة"**
2. اختر الخبر
3. اضغط **"تكملة"**
4. أكمل المحتوى (> 100 حرف)
5. أضف صورة
6. اختر تصنيف
7. اضغط **"حفظ وإرسال"**
8. إذا كان آلي → ينشر تلقائياً ⚡
9. إذا كان تحريري → ينتقل لقسم التحرير 🔵

---

## ⏰ متى ينشر النظام الأخبار الآلية؟

**كل 10 دقائق** يعمل Scheduler تلقائياً:

```typescript
كل 10 دقائق:
1. البحث عن أخبار آلية مكتملة
2. الموافقة عليها تلقائياً
3. نشرها في قسم النشر
```

---

## 🔍 كيف أتحقق من حالة خبر معين؟

```sql
-- في قاعدة البيانات
SELECT 
  rd.id,
  rd.title,
  rd.is_incomplete AS ناقص,
  eq.status AS الحالة,
  EXISTS(SELECT 1 FROM published_items WHERE raw_data_id = rd.id) AS منشور
FROM raw_data rd
LEFT JOIN editorial_queue eq ON rd.id = eq.raw_data_id
WHERE rd.id = رقم_الخبر;
```

---

## 📍 أين يوجد الخبر؟

| المرحلة | الموقع |
|--------|--------|
| بعد السحب | `raw_data` (fetch_status = 'fetched') |
| غير مكتمل | قائمة "أخبار غير مكتملة" |
| ينتظر المراجعة | قسم "التحرير" (status = 'pending') |
| تمت الموافقة | قسم "النشر" (`published_items`) |

---

## ❓ أسئلة شائعة

### لماذا لا يظهر الخبر في قسم النشر؟

**تحقق من**:
- ✅ المحتوى ≥ 100 حرف؟
- ✅ الصورة موجودة؟
- ✅ تمت الموافقة عليه؟
- ✅ لم يُرفض؟

### كم من الوقت يستغرق النشر التلقائي؟

**للأخبار الآلية**: حتى **10 دقائق** (دورة الـ Scheduler)

### هل يمكن نشر خبر تحريري تلقائياً؟

**لا**. الأخبار التحريرية (سياسي/محلي/دولي) تحتاج **موافقة المحرر** دائماً.

---

## 🎯 الخلاصة

```
┌────────────────────────────────────────────┐
│        شروط النشر الأساسية                 │
├────────────────────────────────────────────┤
│  1. المحتوى ≥ 100 حرف          ✅        │
│  2. الصورة موجودة               ✅        │
│  3. الموافقة (محرر أو نظام)     ✅        │
│  4. لم يُرفض                    ✅        │
└────────────────────────────────────────────┘
                    ↓
         قسم النشر (published_items)
                    ↓
           النشر على الموقع الخارجي
```

---

📘 **للتفاصيل الكاملة**: راجع `docs/NEWS_PUBLISHING_FLOW.md`

🎯 **الحالة**: جاهز للاستخدام
