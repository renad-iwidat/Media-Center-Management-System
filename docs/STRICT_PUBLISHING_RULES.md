# 🔒 قواعد النشر الصارمة (بدون استثناءات)

## 🎯 القواعد الجديدة

### ✅ شروط النشر الإلزامية (بدون استثناءات):

```
1️⃣ المحتوى ≥ 300 حرف (إلزامي)
2️⃣ الصورة موجودة (إلزامي)
3️⃣ لا استثناءات لأي source_type (حتى الإدخال اليدوي)
```

---

## 📋 التغييرات المطبقة

### 1️⃣ تغيير الحد الأدنى للمحتوى

#### من:
```typescript
private readonly MIN_CONTENT_LENGTH = 100; // ❌ قديم
```

#### إلى:
```typescript
private readonly MIN_CONTENT_LENGTH = 300; // ✅ جديد
```

**الملف**: `src/services/news/flow-router.service.ts`

---

### 2️⃣ إضافة فحص صارم في الموافقة

**الملف**: `src/services/news/editorial-queue.service.ts`

#### الفحص الجديد:

```typescript
// 🔒 فحص الشروط الصارمة قبل الموافقة (بدون استثناءات)

const MIN_CONTENT_LENGTH = 300;

// 1️⃣ فحص طول المحتوى
if (contentToCheck.length < MIN_CONTENT_LENGTH) {
  return {
    success: false,
    message: `لا يمكن نشر الخبر: المحتوى قصير جداً (${contentToCheck.length} حرف). يجب أن يكون ${MIN_CONTENT_LENGTH} حرف على الأقل.`,
  };
}

// 2️⃣ فحص وجود الصورة
if (!imageToCheck || imageToCheck.trim() === '') {
  return {
    success: false,
    message: 'لا يمكن نشر الخبر: الصورة مفقودة. يجب إضافة صورة للخبر.',
  };
}
```

**النتيجة**: المحرر **لا يستطيع** الموافقة على خبر غير مكتمل (حتى الإدخال اليدوي)

---

### 3️⃣ تحديث سكريبت قاعدة البيانات

**الملف**: `sql/fix_incomplete_articles.sql`

#### من:
```sql
LENGTH(COALESCE(content, '')) < 100  -- ❌ قديم
```

#### إلى:
```sql
LENGTH(COALESCE(content, '')) < 300  -- ✅ جديد
```

---

## 🚫 لا استثناءات

### ❌ قبل التعديل (كان يوجد استثناءات):

```typescript
// الإدخال اليدوي كان له معاملة خاصة
if (USER_INPUT_SOURCE_TYPE_IDS.has(article.source_type_id)) {
  // ⚠️ يمكن تجاوز الشروط
}
```

### ✅ بعد التعديل (لا استثناءات):

```typescript
// الفحص يطبق على الجميع
const MIN_CONTENT_LENGTH = 300;

if (contentToCheck.length < MIN_CONTENT_LENGTH) {
  // ❌ يُرفض - بدون استثناءات
  return { success: false, message: '...' };
}

if (!imageToCheck || imageToCheck.trim() === '') {
  // ❌ يُرفض - بدون استثناءات
  return { success: false, message: '...' };
}
```

---

## 📊 ماذا يحدث الآن؟

### 1️⃣ عند سحب الأخبار:

```
خبر جديد
  ↓
فحص: المحتوى ≥ 300 حرف؟ الصورة موجودة؟
  ↓
┌─────────────────┬─────────────────┐
│  ✅ نعم         │  ❌ لا          │
│  مكتمل          │  غير مكتمل     │
│  is_incomplete  │  is_incomplete  │
│  = false        │  = true         │
└─────────────────┴─────────────────┘
```

### 2️⃣ عند محاولة الموافقة:

```
المحرر يضغط "موافقة"
  ↓
فحص: المحتوى ≥ 300 حرف؟ الصورة موجودة؟
  ↓
┌─────────────────┬─────────────────┐
│  ✅ نعم         │  ❌ لا          │
│  تتم الموافقة  │  يُرفض         │
│  يُنشر الخبر    │  رسالة خطأ     │
└─────────────────┴─────────────────┘
```

### 3️⃣ رسالة الخطأ للمحرر:

#### إذا المحتوى قصير:
```
❌ لا يمكن نشر الخبر: المحتوى قصير جداً (136 حرف). يجب أن يكون 300 حرف على الأقل.
```

#### إذا الصورة مفقودة:
```
❌ لا يمكن نشر الخبر: الصورة مفقودة. يجب إضافة صورة للخبر.
```

---

## 🔄 تطبيق التعديلات

### الخطوة 1️⃣: تحديث قاعدة البيانات

```bash
# تطبيق المعايير الجديدة (300 حرف)
psql "DATABASE_URL" -f sql/fix_incomplete_articles.sql
```

### الخطوة 2️⃣: إعادة تشغيل Backend

```bash
# إعادة تشغيل الخادم لتطبيق الكود الجديد
npm run dev
```

### الخطوة 3️⃣: التحقق

```sql
-- تحقق من الأخبار غير المكتملة الجديدة
SELECT 
  COUNT(*) AS total,
  COUNT(CASE WHEN LENGTH(content) < 300 THEN 1 END) AS short_content,
  COUNT(CASE WHEN image_url IS NULL OR TRIM(image_url) = '' THEN 1 END) AS no_image
FROM raw_data
WHERE is_incomplete = true;
```

---

## 📈 التأثير المتوقع

### قبل التعديل:
```
✅ 100 حرف = مكتمل
❌ استثناء للإدخال اليدوي
⚠️ يمكن نشر أخبار قصيرة
```

### بعد التعديل:
```
✅ 300 حرف = مكتمل
✅ لا استثناءات لأي نوع
✅ جودة محتوى أفضل
✅ كل الأخبار بصور
```

---

## 🎯 الفوائد

### 1. جودة المحتوى
```
300 حرف = محتوى أكثر تفصيلاً
← أخبار أكثر إفادة
← تجربة قارئ أفضل
```

### 2. الاتساق
```
نفس القواعد للجميع
← لا استثناءات
← لا ارتباك
```

### 3. الصور الإلزامية
```
كل خبر له صورة
← محتوى مرئي جذاب
← مظهر احترافي
```

---

## ⚠️ ملاحظات مهمة

### 1. الأخبار الموجودة

```sql
-- الأخبار المنشورة بالفعل لن تتأثر
-- فقط الأخبار الجديدة وما سيُنشر مستقبلاً
```

### 2. الأخبار العاجلة

```
إذا كان هناك خبر عاجل قصير:
1. أضف تفاصيل إضافية (للوصول لـ 300 حرف)
2. أضف سياق أو خلفية
3. أضف صورة مناسبة
```

### 3. الإدخال اليدوي

```
الإدخال اليدوي الآن:
❌ لا يمكن تجاوز القواعد
✅ نفس الشروط للجميع
✅ 300 حرف + صورة إلزامية
```

---

## 🔍 اختبار القواعد الجديدة

### اختبار 1: محتوى قصير

```typescript
// محاولة الموافقة على خبر 150 حرف
const result = await approveItem(queueId, ...);

// النتيجة المتوقعة:
{
  success: false,
  message: "لا يمكن نشر الخبر: المحتوى قصير جداً (150 حرف). يجب أن يكون 300 حرف على الأقل."
}
```

### اختبار 2: بدون صورة

```typescript
// محاولة الموافقة على خبر بدون صورة
const result = await approveItem(queueId, ...);

// النتيجة المتوقعة:
{
  success: false,
  message: "لا يمكن نشر الخبر: الصورة مفقودة. يجب إضافة صورة للخبر."
}
```

### اختبار 3: مكتمل

```typescript
// خبر 350 حرف + صورة
const result = await approveItem(queueId, ...);

// النتيجة المتوقعة:
{
  success: true,
  message: "تمت الموافقة على الخبر ونشره بنجاح"
}
```

---

## 📊 الإحصائيات المتوقعة

### قبل التعديل (100 حرف):
```
أخبار غير مكتملة: ~20 خبر
أسباب: معظمها بدون صور
```

### بعد التعديل (300 حرف):
```
أخبار غير مكتملة: ~50-80 خبر
أسباب: محتوى قصير + بدون صور
```

**الزيادة طبيعية** لأن المعايير أصبحت أعلى ✅

---

## 🎯 الخلاصة

```
┌────────────────────────────────────────────┐
│        القواعد الصارمة الجديدة            │
├────────────────────────────────────────────┤
│                                            │
│  1. المحتوى ≥ 300 حرف       ✅ إلزامي   │
│  2. الصورة موجودة            ✅ إلزامي   │
│  3. لا استثناءات             ✅ الجميع   │
│  4. ينطبق على الإدخال اليدوي ✅ نعم      │
│                                            │
└────────────────────────────────────────────┘
```

### النتيجة:
- ✅ محتوى أفضل جودة
- ✅ كل الأخبار بصور
- ✅ لا استثناءات
- ✅ قواعد واضحة وثابتة

---

📅 **تاريخ التعديل**: يونيو 2026  
👤 **المطور**: Kiro AI Assistant  
🎯 **الحالة**: مطبق ✅
