services:
  - type: web
    name: mcms-frontend
    env: node
    region: oregon
    plan: free
    buildCommand: npm run frontend:build
    startCommand: npm run frontend:serve
    envVars:
      - key: VITE_API_URL
        value: https://media-center-management-system.onrender.com
