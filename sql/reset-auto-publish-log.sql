-- ═══════════════════════════════════════════════════════════════
-- حذف سجل النشر التلقائي (auto_publish_log) لإعادة التجربة
-- هذا يسمح للنظام بإعادة نشر نفس الأخبار مرة ثانية
-- ═══════════════════════════════════════════════════════════════

-- حذف كل سجلات النشر التلقائي
DELETE FROM auto_publish_log;

-- تأكيد
SELECT 'تم حذف سجل النشر التلقائي — الأخبار ستُنشر مجدداً في الدورة القادمة' AS result;
SELECT COUNT(*) AS remaining FROM auto_publish_log;
